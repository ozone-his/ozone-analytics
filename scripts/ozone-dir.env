# Where the fetched Ozone distro lives. A pre-set OZONE_DIR wins, so it is configurable without
# editing this file; the default keeps the historical layout for anyone not overriding it.
export OZONE_DIR="${OZONE_DIR:-$PWD/distro}"
